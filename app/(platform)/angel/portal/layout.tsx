/**
 * Angel Portal layout — all sub-pages are now open for angel members.
 */
export default function AngelPortalLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
